import {styled} from "@mui/material";

export const WTLBaseFgContainer = styled('div')({
    overflowX: 'hidden',
    overflowY: 'hidden',
    height: 573,
    flexDirection: 'column',
    gap: 1,
    position: 'absolute',
    top: 0,
    left: 0,
    zIndex: 100,
})